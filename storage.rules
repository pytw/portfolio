service firebase.storage {
  match /b/{bucket}/o {
    match /projects_img/{imageName} {
      allow read: if true;  // Public access to images
      allow write: if request.auth != null;  // Only authenticated users can upload
    }
  }
}
